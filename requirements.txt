aiogram==3.13.1
aiosqlite==0.20.0
cryptography==43.0.3
httpx==0.27.2
openai==1.54.3
anthropic==0.39.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1
# извлечение текста из документов (все pure-Python — работают на Termux)
pypdf==5.1.0
docx2txt==0.8
openpyxl==3.1.5
striprtf==0.0.26
# OCR: рендеринг PDF-страниц в картинки для скан-PDF без текста.
# pypdfium2 поставляется как self-contained wheel с PDFium-бинарём,
# работает на linux-aarch64 (Termux) без сборки из исходников.
pypdfium2==4.30.0
